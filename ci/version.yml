platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 7-jre-alpine
  
inputs:
- name: code-repo

#outputs:
#- name: tests

run:
  path: sh
  args: ['./web-ui/ci/scripts/version.sh']